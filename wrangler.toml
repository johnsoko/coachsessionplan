name = "coachsessionplan"
compatibility_date = "2025-01-01"
compatibility_flags = ["nodejs_compat"]
main = "src/worker.js"

# static site (index.html etc.) — served for any request that isn't /api/*
[assets]
directory = "."
binding = "ASSETS"

[[d1_databases]]
binding = "DB"
database_name = "coachsessionplan-db"
database_id = "1e5a4aef-ce47-434c-a017-07c790d94475"

# Clerk publishable key isn't secret — safe to keep in this file.
# CLERK_SECRET_KEY is NOT set here — set it with:
#   npx wrangler secret put CLERK_SECRET_KEY
[vars]
CLERK_PUBLISHABLE_KEY = "pk_test_ZGVhci1iYXNzLTcyNTYuY2xlcmsuYWNjb3VudHMuZGV2JA"
