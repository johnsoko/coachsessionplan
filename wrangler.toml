name = "coachsessionplan"
compatibility_date = "2025-01-01"
main = "src/worker.js"

# static site (index.html etc.) — served for any request that isn't /api/*
[assets]
directory = "."
binding = "ASSETS"

# fill in database_id after running:
#   npx wrangler d1 create coachsessionplan-db
[[d1_databases]]
binding = "DB"
database_name = "coachsessionplan-db"
database_id = "1e5a4aef-ce47-434c-a017-07c790d94475"