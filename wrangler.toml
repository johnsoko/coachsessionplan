name = "coachsessionplan"
compatibility_date = "2025-01-01"

# this project deploys as a Worker serving static assets (not a classic
# Pages project) — this tells wrangler where those static files live
[assets]
directory = "."

# Once we add a backend, D1 binding goes here, e.g.:
# [[d1_databases]]
# binding = "DB"
# database_name = "coachsessionplan-db"
# database_id = "REPLACE_AFTER_CREATING_DB"